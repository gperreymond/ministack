name: 'hashistack-old'
datacenter: 'local'
image:
  repository: 'ghcr.io/gperreymond/hashibase'
  tag: 'base-1.0.0'

services:
  consul:
    enabled: false
  nomad:
    enabled: true
    version: '1.8.2'
    replicas: 3
    clients: false
  vault:
    enabled: false

plugins:
  traefik:
    enabled: true
    version: '3.2.3'
